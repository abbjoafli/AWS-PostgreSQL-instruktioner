{
  "name": "postgressql",
  "version": "1.0.0",
  "description": "En postgresSQL databas",
  "main": "index.js",
  "scripts": {
    "start": "set NODE_ENV=dev && node server.js",
    "dev": "set NODE_ENV=dev && nodemon  server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
